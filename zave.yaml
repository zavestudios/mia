apiVersion: zave.io/v1
kind: Workload
metadata:
  name: autonomous-agent

spec:
  runtime: container
  exposure: internal-http
  delivery: rolling

  build:
    mode: dockerfile

  capabilities:
    - name: tracing

  resources:
    tier: standard
