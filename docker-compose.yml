services:
  mia:
    build: .
    ports:
      - "18789:18789"
    volumes:
      - ./config:/root/.openclaw
      - openclaw_data:/root/.openclaw/data
    environment:
      - NODE_ENV=development

volumes:
  openclaw_data:
